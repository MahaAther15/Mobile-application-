# TODO - Neon Admin Panel Upgrade

- [ ] Update `lib/app_theme.dart`: enhance `neonTheme` to a full cohesive neon admin look (cards, inputs, dividers, popup menu, selection).
- [ ] Update `lib/screens/admin_dashboard_screen.dart`: add neon pulse/glow for title + cards; add animated scanline/grid background; animate order cards on entry.
- [ ] Run `flutter analyze` to ensure build correctness.
- [ ] Run the app (or at least build) to verify UI/animations.

